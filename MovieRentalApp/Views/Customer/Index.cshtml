@model IEnumerable<CustomerViewModel>

<a class="btn btn-success mt-5" style="margin: 10px" asp-controller="Customer" asp-action="Create">Create new customer</a>

<table class="table table-striped table-hover ">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Rented movie count</th>
            <th>Edit customer</th>
            <th>Delete customer</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customerVM in Model)
        {
            <tr>
                <td>@customerVM.Customer.Id</td>
                <td>@customerVM.Customer.Name</td>
                <td>@customerVM.MovieCount</td>
                <td><a class="btn btn-info" asp-controller="Customer" asp-action="Update" asp-route-id="@customerVM.Customer.Id">Edit</a></td>
                <td><a class="btn btn-danger"  asp-controller="Customer" asp-action="Delete" asp-route-id="@customerVM.Customer.Id">Delete</a></td>
            </tr>
        }
    </tbody>
</table>