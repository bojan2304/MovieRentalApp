@model RentalViewModel
<div class="row orderForm">

    <form asp-controller="Rental" asp-action="RentMovie" method="post" class="form-horizontal" role="form">
        <h3 class="col-md-offset-2">Rent a movie</h3>
        @Html.HiddenFor(m => Model.Movie.Id)
        <div class="form-group">
            <label asp-for="Movie.Borrower" class="col-md-2 control-label"></label>
            <div class="col-md-6">

                @Html.DropDownListFor(m => m.Movie.BorrowerId,
                                             new SelectList(Model.Customers.Select(x => new { Value = x.Id, Text = x.Name }), "Value", "Text"),
                                             "Select Customer",
                                             htmlAttributes: new { @class = "form-control border-input" })
            </div>
        </div>
        <div class="form-group">
            <div class="btn-group col-md-offset-2 col-md-6">
                <input class="btn btn-default" type="submit" value="Return" asp-controller="Rental" asp-action="Index" />
                <input class="btn btn-success" type="submit" value="Rent" />
            </div>
        </div>
    </form>
</div>